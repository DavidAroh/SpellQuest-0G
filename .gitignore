# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — never commit real keys
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude worktrees (local only)
.claude/worktrees/

# Large art source files (kept locally, not shipped)
art-source/

# Video render work dir (large intermediates)
videos/
_frames/

# Rendered demo (upload to a host for the submission Video URL)
SpellQuest-demo.mp4

# Generated brand assets (upload to host; not shipped)
spellquest-logo.png
spellquest-thumb.png
spellquest-cover*.png
